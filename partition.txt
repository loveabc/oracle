CREATE TABLE TEST_PART_LOG
(request_id number,
status_date DATE
)
PARTITION BY RANGE (STATUS_DATE)
INTERVAL (NUMTOYMINTERVAL(1,'year'))
(

PARTITION part1 VALUES LESS THAN (TO_DATE ('2018-01-01', 'YYYY-MM-DD'))

);

SELECT * FROM TEST_PART PARTITION(PART1);
SELECT * FROM TEST_PART_LOG PARTITION(SYS_P23);
